FROM node:20-slim

# Install Python
RUN apt-get update && apt-get install -y python3 python3-pip python3-venv && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Install Node dependencies
COPY package*.json ./
RUN npm ci --production

# Install Python dependencies
COPY scraper/requirements.txt ./scraper/
RUN python3 -m venv /app/venv && /app/venv/bin/pip install --no-cache-dir -r scraper/requirements.txt
RUN /app/venv/bin/playwright install chromium --with-deps

# Copy all source
COPY . .

# Start script: launches Python scraper + Node backend
COPY start.sh ./
RUN chmod +x start.sh

EXPOSE 3001
CMD ["./start.sh"]
